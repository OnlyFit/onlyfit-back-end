package ieti.project.onlyfit.error;

public enum ErrorCodeEnum
{
    USER_NOT_FOUND,
    USER_WITH_EMAIL_ALREADY_EXISTS,
    EXPIRED_TOKEN,
    INVALID_USER_CREDENTIALS,
    USER_ALREADY_CREATED,
    ROUTINES_NOT_FOUND
}
